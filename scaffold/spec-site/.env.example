# spec-site Environment Configuration
#
# Mode: "static" (no backend) or "api" (full interactive)
VITE_SPEC_MODE=static

# API URL (required for "api" mode)
# Examples:
#   Turso+CF Workers: https://pm-api.my-project.workers.dev
#   Supabase:         https://abc123.supabase.co/functions/v1
#   Lambda:           https://api.my-project.com
# VITE_API_URL=
